<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.dai1678.returntimesapp">

    <!-- ネットワークベース(Networkを使って位置情報を取得する場合は以下の２つを追加)(INTERNETは通信が発生するので必要) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <!-- GPSベース -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyCWQEu_-ghxt5na0m1NnKl0SNZ85c9CQPc" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name="org.dai1678.returntimesapp.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".HomeActivity" />

        <activity
            android:name="org.dai1678.returntimesapp.SettingMapsActivity"
            android:label="@string/title_activity_setting_maps" />

        <activity android:name="org.dai1678.returntimesapp.SetDestinationActivity" />
        <activity android:name="org.dai1678.returntimesapp.SetMailDetailActivity" />
        <activity android:name="org.dai1678.returntimesapp.SettingProfileActivity"/>
    </application>

</manifest>